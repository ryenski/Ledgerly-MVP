# Local desktop app with cross-platform path

Ledgerly will be built as a local desktop app that manages local Workspace folders and preserves a path to cross-platform V1.0. The MVP may target macOS first, but the architecture should avoid Mac-only assumptions; Tauri and Electron are preferred candidates over a browser-first local web app unless a compelling implementation reason emerges.
